// borrow_mut_inmut.fc — DEBE FALLAR
// ESPERADO: [O002]
// Spec: borrow mutable + inmutable simultáneo = O002 (borrow conflict) en N2

función principal() -> Entero32 estricto {
    el x: Entero32 = 42;
    el ref_mut: &mut Entero32 = &mut x;
    la ref: &Entero32 = &x;
    retornar *ref;
}
