[package]
name = "falcato_runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
# Cripto (R8.2): NUNCA inventar cripto a mano — crates auditadas, puras Rust.
# blake3: hash de contenido (integridad, Capa 1 de R8.3)
# ed25519-dalek: firma de editor (autenticidad, Capa 3 de R8.3)
blake3 = "1.5"
ed25519-dalek = "2"
rand = "0.8"
